import { Injectable } from '@angular/core';

/**
 * Servicio de autenticación.
 *
 * Esqueleto de arquitectura: sin lógica por ahora. La implementación real
 * (incluida la conexión con el backend FastAPI) se desarrollará en una fase
 * posterior del proyecto.
 */
@Injectable({ providedIn: 'root' })
export class AuthService {}
